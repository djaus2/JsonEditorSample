<Window x:Class="JsonEditorSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JSON Editor" Height="600" Width="500">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="MenuOpen_Click" />
                <MenuItem Header="_Save" Click="MenuSave_Click" />
            </MenuItem>
        </Menu>
        <Grid>
            <TreeView ItemsSource="{Binding RootNodes}" Name="JsonTreeView" Margin="10">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="5" />
                            <TextBlock Text="{Binding Key}" Width="100" Margin="0,0,10,0" VerticalAlignment="Center" TextAlignment="Right"/>
                            <TextBlock Text=" " Width="5" Margin="0,0,10,0" VerticalAlignment="Center" TextAlignment="Left"/>
                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="350"
                                     Background="{Binding IsValid, Converter={StaticResource BoolToBrushConverter}}"
                                     VerticalAlignment="Center" TextAlignment="Left"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>
    </DockPanel>
</Window>